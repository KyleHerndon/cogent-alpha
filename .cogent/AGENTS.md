# .cogent/

This directory holds the cogent's persistent state — identity, memory, and session data.

## Required: IDENTITY.md

`.cogent/IDENTITY.md` must exist for the cogent to be valid. Without it, the cogent is uninitialized and cannot run `/improve`, `/wakeup`, or any other skill that depends on identity.

Run `/initialize` to create it.
